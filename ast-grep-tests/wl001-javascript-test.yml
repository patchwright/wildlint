id: wl001-javascript-replace-as-strip
# else / else-if cases are PERMANENT fixtures (round-1 + 0.7.2 else-branch FP).
invalid:
  - |-
    if (s.startsWith("/blob/")) {
        s = s.replaceAll("/blob/", "");
    }
valid:
  - |-
    s = s.replaceAll("/blob/", "");
  - |-
    if (s.startsWith("/blob/")) {
        s = s.replaceAll("/x/", "");
    }
  - |-
    if (s.startsWith("/blob/")) {
    } else {
        s = s.replaceAll("/blob/", "");
    }
  - |-
    if (s.startsWith("/blob/")) {
    } else if (other) {
        s = s.replaceAll("/blob/", "");
    }
  - |-
    s = s.replace("/blob/", "");
  - |-
    s = s.startsWith("/blob/") ? s.slice(6) : s
