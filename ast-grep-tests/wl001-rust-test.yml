id: wl001-rust-replace-as-strip
# else / else-if cases are PERMANENT fixtures (round-1 + 0.7.2 else-branch FP).
invalid:
  - |-
    if s.starts_with("/blob/") {
        let x = s.replace("/blob/", "");
    }
valid:
  - |-
    let x = s.replace("/blob/", "");
  - |-
    if s.starts_with("/blob/") {
        let x = s.replace("/x/", "");
    }
  - |-
    if s.starts_with("/blob/") {
    } else {
        let x = s.replace("/blob/", "");
    }
  - |-
    if s.starts_with("/blob/") {
    } else if other {
        let x = s.replace("/blob/", "");
    }
  - |-
    let x = s.strip_prefix("/blob/");
